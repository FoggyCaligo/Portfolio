<script setup lang="ts">
import Format from '../components/SkillsPage/Format.vue'

const hclass = 'text-yellow pt-10'

</script>


<template>

    <div class="text-2xl font-bold h-[calc(100dvh-var(--nav-h))]  overflow-y-scroll scrollbar-hide">
      <h1 :class="hclass">Tooling</h1>
      <Format title="JavaScript / HTML / CSS">
          브라우저에서 사용자의 입력과 상태 변화에 따라 화면이 동적으로 반응하도록 구현하는 데 주력해 왔습니다. <br/>
          단순한 스타일링을 넘어서, 데이터 변화가 UI에 어떻게 반영되는지를 기준으로 화면 구조를 설계했습니다.
      </Format>
      <Format title="Vue.js (Vue 3)">
          컴포넌트 단위로 화면을 분리하고, props와 emits를 활용해 데이터 흐름을 명확히 관리하는 방식에 익숙합니다. <br/>
          상태 변화에 따른 렌더링 타이밍을 고려하며, 유지보수 시 의도를 파악하기 쉬운 구조를 만드는 것을 중요하게 생각합니다.
      </Format>
      <Format title="Axios">
          백엔드에서 제공되는 API 명세서(apidog 기반)를 해석해 필요한 파라미터와 응답 구조를 분석하고,<br/>
          Axios를 통해 데이터 연동을 구현했습니다.<br/>
          기존 프로젝트의 Axios 인스턴스 및 인터셉터 구조를 이해한 후, 동일한 패턴을 유지하며 API 호출 로직을 작성했습니다.
      </Format>
      <Format title="Vitest">
          컴포넌트 단위로 테스트 코드를 작성하며,<br/>
          코드 변경 시 기존 기능이 의도치 않게 깨지지 않도록 안정성을 확보하는 경험을 했습니다.
      </Format>

      <h1 :class="hclass">DevOps / Environment</h1>
      <Format title="Git / GitHub">
          개인 프로젝트와 실무 환경 모두에서 Git을 사용해 버전 관리를 진행했습니다.<br/>
          기능 단위로 커밋을 나누고, 기존 흐름을 해치지 않도록 수정 범위를 최소화하는 방식으로 작업해 왔습니다.
      </Format>
      <Format title="Vite">
          Vite 기반 프로젝트에서 빌드 및 배포 환경 차이로 발생하는 자산 로딩 문제를 경험했고,
          import 기반 자산 관리 방식으로 이를 해결하며 번들링 구조에 대한 이해를 넓혔습니다.
      </Format>
      <Format title="개발 환경">
          주로 VSCode를 사용해 왔으며, Git 사용 흐름의 편의성을 고려해 WebStorm을 병행 사용하고 있습니다.
      </Format>
      
      <h1 :class="hclass">경험 기반 보충</h1>
      <Format title="실무 프로젝트 경험">
          간병인–보호자 매칭 서비스 메디25케어의 프론트엔드 개발에 참여해,<br/>
          간병인 지원 파트 전반의 UI 구현과 유지보수를 담당했습니다.<br/>
          기획 문서가 충분하지 않은 상황에서 기존 코드를 분석하며 구조를 이해하고,<br/>
          기능 추가 시 전체 흐름에 미치는 영향을 고려하는 경험을 했습니다. <br/>
      </Format>
      <Format title="캡스톤 디자인 프로젝트">
          Python과 증권 API를 활용해 자동 트레이딩 프로그램을 개발하며,<br/>
          실시간 데이터 흐름을 구조화하고 조건에 따라 동작하는 로직을 설계했습니다.<br/>
          해당 프로젝트로 대전권 대학연합 산학협력 성과공유 발표 경진대회에서 수상했습니다. <br/>
      </Format>
      <Format title="문제 접근 방식">
          새로운 기술이나 코드베이스를 접했을 때,<br/>
          기능 구현보다 먼저 구조와 의도를 파악하려는 접근 방식을 유지하고 있습니다.<br/>
      </Format>

    </div>
  </template>
  


  <!--
🔹 Tooling

JavaScript / HTML / CSS
브라우저에서 사용자의 입력과 상태 변화에 따라 화면이 동적으로 반응하도록 구현하는 데 주력해 왔습니다.
단순한 스타일링을 넘어서, 데이터 변화가 UI에 어떻게 반영되는지를 기준으로 화면 구조를 설계했습니다.

Vue.js (Vue 3)
컴포넌트 단위로 화면을 분리하고, props와 emits를 활용해 데이터 흐름을 명확히 관리하는 방식에 익숙합니다.
상태 변화에 따른 렌더링 타이밍을 고려하며, 유지보수 시 의도를 파악하기 쉬운 구조를 만드는 것을 중요하게 생각합니다.

Axios
백엔드에서 제공되는 API 명세서(apidog 기반)를 해석해 필요한 파라미터와 응답 구조를 분석하고,
Axios를 통해 데이터 연동을 구현했습니다.
기존 프로젝트의 Axios 인스턴스 및 인터셉터 구조를 이해한 후, 동일한 패턴을 유지하며 API 호출 로직을 작성했습니다.

Vitest
컴포넌트 단위로 테스트 코드를 작성하며,
코드 변경 시 기존 기능이 의도치 않게 깨지지 않도록 안정성을 확보하는 경험을 했습니다.

🔹 DevOps / Environment

Git / GitHub
개인 프로젝트와 실무 환경 모두에서 Git을 사용해 버전 관리를 진행했습니다.
기능 단위로 커밋을 나누고, 기존 흐름을 해치지 않도록 수정 범위를 최소화하는 방식으로 작업해 왔습니다.

Vite
Vite 기반 프로젝트에서 빌드 및 배포 환경 차이로 발생하는 자산 로딩 문제를 경험했고,
import 기반 자산 관리 방식으로 이를 해결하며 번들링 구조에 대한 이해를 넓혔습니다.

개발 환경
주로 VSCode를 사용해 왔으며, Git 사용 흐름의 편의성을 고려해 WebStorm을 병행 사용하고 있습니다.

🔹 경험 기반 보충

실무 프로젝트 경험
간병인–보호자 매칭 서비스 메디25케어의 프론트엔드 개발에 참여해,
간병인 지원 파트 전반의 UI 구현과 유지보수를 담당했습니다.
기획 문서가 충분하지 않은 상황에서 기존 코드를 분석하며 구조를 이해하고,
기능 추가 시 전체 흐름에 미치는 영향을 고려하는 경험을 했습니다. 

이력서_20251217

캡스톤 디자인 프로젝트
Python과 증권 API를 활용해 자동 트레이딩 프로그램을 개발하며,
실시간 데이터 흐름을 구조화하고 조건에 따라 동작하는 로직을 설계했습니다.
해당 프로젝트로 대전권 대학연합 산학협력 성과공유 발표 경진대회에서 수상했습니다. 

이력서_20251217

문제 접근 방식
새로운 기술이나 코드베이스를 접했을 때,
기능 구현보다 먼저 구조와 의도를 파악하려는 접근 방식을 유지하고 있습니다.
  
  
  
  
  
  
  -->